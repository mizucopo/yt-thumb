name: Docker Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  docker-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Read version
        id: version
        run: |
          echo "version=$(cat version)" >> $GITHUB_OUTPUT

      - name: Read git author
        id: author
        run: |
          echo "name=$(git log -1 --format=%an)" >> $GITHUB_OUTPUT
          echo "email=$(git log -1 --format=%ae)" >> $GITHUB_OUTPUT

      - name: Configure Git
        run: |
          git config user.name "${{ steps.author.outputs.name }}"
          git config user.email "${{ steps.author.outputs.email }}"

      - name: Check if tag exists
        run: |
          git fetch --tags
          if git rev-parse "${{ steps.version.outputs.version }}" >/dev/null 2>&1; then
            exit 1
          fi

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: mizucopo
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            mizucopo/yt-thumb:latest
            mizucopo/yt-thumb:${{ steps.version.outputs.version }}

      - name: Create and push git tag
        run: |
          git tag -a "${{ steps.version.outputs.version }}" -m "Release ${{ steps.version.outputs.version }}"
          git push origin "${{ steps.version.outputs.version }}"

      - name: Create GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ steps.version.outputs.version }}" \
            --title "Release ${{ steps.version.outputs.version }}" \
            --notes "## Docker Image

          Pull the Docker image:

          \`\`\`bash
          docker pull mizucopo/yt-thumb:${{ steps.version.outputs.version }}
          \`\`\`

          Or use the latest tag:

          \`\`\`bash
          docker pull mizucopo/yt-thumb:latest
          \`\`\`

          ## Links:
          - [Docker Hub](https://hub.docker.com/r/mizucopo/yt-thumb)
          - [Source Code](${{ github.server_url }}/${{ github.repository }}/tree/${{ steps.version.outputs.version }})
          "
